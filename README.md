# Hubbly - Платформа чата с 3D аватарами

**Реальное время общения с 3D аватарами на SignalR и Three.js**

[![.NET](https://img.shields.io/badge/.NET-9.0-blue)](https://dotnet.microsoft.com/)
[![SignalR](https://img.shields.io/badge/SignalR-Real--time-green)](https://dotnet.microsoft.com/apps/aspnet/signalr)
[![Three.js](https://img.shields.io/badge/Three.js-3D-orange)](https://threejs.org/)
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-База%20данных-blue)](https://www.postgresql.org/)

---

## 🌟 Возможности

- **Об messaging в реальном времени**: Мгновенная доставка сообщений через SignalR WebSockets
- **3D Аватары**: Интерактивная система 3D аватаров на Three.js
- **Кастомизация аватаров**: Персонализация аватаров различной одеждой, прическами и аксессуарами
- **Система комнат**: Автоматическое распределение по комнатам с очисткой пустых
- **JWT Аутентификация**: Безопасная токен-базированная аутентификация с refresh токенами
- **Ограничение запросов**: Защита от злоупотреблений с настраиваемыми лимитами
- **Health Checks**: Комплексный мониторинг базы данных, 3D моделей и SignalR хаба
- **CI/CD Ready**: GitHub Actions workflow для zero-downtime деплоя
- **Адаптивный дизайн**: Работает на десктопе и мобильных устройствах

---

## 🏗️ Архитектура

Hubbly следует принципам **Clean Architecture** с четким разделением ответственности:

```
Hubbly.Backend/
├── Hubbly.Domain/           # Основные бизнес-сущности и интерфейсы
│   ├── Entities/           # User, ChatRoom, RefreshToken, AvatarConfig
│   ├── Dtos/               # Data transfer objects
│   └── Services/           # Интерфейсы доменных сервисов
├── Hubbly.Application/     # Use cases и бизнес-логика
│   └── Services/          # Реализации сервисов
├── Hubbly.Infrastructure/  # Доступ к данным и внешние зависимости
│   ├── Data/              # Entity Framework DbContext
│   ├── Migrations/        # Миграции базы данных
│   └── Repositories/      # Реализации репозиториев
└── Hubbly.Api/            # Web API и SignalR хаб
    ├── Controllers/       # REST API endpoints
    ├── Hubs/              # SignalR ChatHub
    ├── Middleware/        # Логирование запросов, обработка исключений
    └── wwwroot/           # Статические файлы (3D модели, фронтенд)
```

**Ключевые паттерны:**
- Dependency Injection везде
- Repository pattern для доступа к данным
- Interface-based design для тестируемости
- Background services для фоновых задач

---

## 🚀 Технологический стек

### Backend
- **Фреймворк**: ASP.NET Core 8.0
- **База данных**: PostgreSQL с Entity Framework Core
- **Real-time**: SignalR с WebSockets
- **Аутентификация**: JWT Bearer токены
- **Кэширование**: MemoryCache с лимитами размера
- **Отказоустойчивость**: Polly retry политики
- **Валидация**: FluentValidation паттерны

### Frontend (3D)
- **3D Движок**: Three.js r121
- **Анимации**: Tween.js
- **Модели**: GLTF/GLB формат
- **UI**: Vanilla JavaScript с ES6 модулями

### DevOps
- **CI/CD**: GitHub Actions
- **Деплой**: Zero-downtime с health checks
- **Мониторинг**: Встроенные health endpoints
- **Контейнеризация**: Готово для Docker

---

## 📋 Требования

- [.NET 9.0 SDK](https://dotnet.microsoft.com/download/dotnet/9.0)
- [PostgreSQL 14+](https://www.postgresql.org/download/)
- Node.js (опционально, для разработки фронтенда)

---

## ⚡ Быстрый старт

### 1. Клонировать репозиторий
```bash
git clone https://github.com/yourusername/Hubbly.Backend.git
cd Hubbly.Backend
```

### 2. Настроить окружение
```bash
# Скопировать шаблон конфигурации
cp Hubbly.Api/appsettings.json.example Hubbly.Api/appsettings.json

# Отредактировать конфигурацию
nano Hubbly.Api/appsettings.json
```

**Обязательные настройки:**
```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5432;Database=hubbly;Username=postgres;Password=yourpassword"
  },
  "Jwt": {
    "Secret": "your-super-secret-jwt-key-min-32-chars",
    "Issuer": "Hubbly",
    "Audience": "HubblyUsers",
    "ExpiryInMinutes": 15
  }
}
```

### 3. Применить миграции базы данных
```bash
dotnet ef database update --project Hubbly.Infrastructure --startup-project Hubbly.Api
```

### 4. Запустить приложение
```bash
# Development
dotnet run --project Hubbly.Api

# Или с hot reload
dotnet watch run --project Hubbly.Api
```

### 5. Доступ к приложению
- **API**: http://localhost:5000
- **Health Checks**:
  - http://localhost:5000/health/ready (готовность)
  - http://localhost:5000/health/live (живучесть)
- **SignalR Hub**: http://localhost:5000/chatHub
- **3D Фронтенд**: http://localhost:5000/three_scene.html

---

## 🔧 Конфигурация

### CORS Origins
Настройте разрешенные origins в `appsettings.json`:
```json
{
  "Cors": {
    "AllowedOrigins": [
      "http://localhost:5000",
      "http://127.0.0.1:5000",
      "http://your-mobile-app-ip:5000"
    ]
  }
}
```

### Настройки комнат
```json
{
  "Rooms": {
    "DefaultMaxUsers": 50,
    "EmptyThresholdMinutes": 30
  }
}
```

### Ограничение запросов
- **Глобально**: 100 запросов в минуту на пользователя/IP
- **Auth endpoints**: 5 запросов в минуту

---

## 📡 API Endpoints

### Аутентификация
| Метод | Endpoint | Описание |
|-------|----------|---------|
| POST | `/api/auth/guest` | Аутентификация как гость (создает пользователя) |
| POST | `/api/auth/refresh` | Обновить access токен |

### Пользователь (Аутентифицированный)
| Метод | Endpoint | Описание |
|-------|----------|---------|
| GET | `/api/user/me` | Получить профиль текущего пользователя |
| PUT | `/api/user/nickname` | Обновить никнейм |
| PUT | `/api/user/avatar` | Обновить конфигурацию аватара |

### SignalR События
| Событие | Направление | Описание |
|---------|-------------|---------|
| `SendMessage` | Client → Server | Отправить сообщение в чат |
| `ReceiveMessage` | Server → Client | Получить сообщение |
| `UserJoined` | Server → Client | Пользователь зашел в комнату |
| `UserLeft` | Server → Client | Пользователь вышел из комнаты |
| `UserTyping` | Server → Client | Пользователь печатает |
| `UserPlayAnimation` | Server → Client | Воспроизвести анимацию аватара |
| `AssignedToRoom` | Server → Client | Назначение комнаты |
| `ReceiveError` | Server → Client | Уведомление об ошибке |

---

## 🎨 3D Система аватаров

### Структура аватара
- **Базовые модели**: Мужские и женские базовые аватары
- **Кастомизация**:
  - Верхняя одежда (футболки, худи, куртки и т.д.)
  - Нижняя одежда (джинсы, шорты, юбки и т.д.)
  - Прически (лысый, боб, конский хвост, могавк и т.д.)
  - Обувь (ботинки, сандалии, кроссовки)
- **Анимации**: idle (бездействие), clapping (хлопки), waving (махание)

### Формат моделей
- **Формат**: GLTF/GLB (бинарный)
- **Расположение**: `Hubbly.Api/wwwroot/avatars/` и `animations/`
- **Масштаб**: 1.8x равномерное масштабирование
- **Система координат**: Y-up, ноги на Y=0

### Добавление новых ассетов
1. Поместите GLB файлы в соответствующие директории
2. Обновите валидацию `AvatarConfig` при добавлении новых категорий
3. Модели автоматически отдаются с `/avatars/` и `/animations/`

---

## 🗄️ Схема базы данных

### Пользователи (Users)
```sql
CREATE TABLE users (
    id UUID PRIMARY KEY,
    device_id VARCHAR(255) UNIQUE NOT NULL,
    nickname VARCHAR(50),
    avatar_config_json TEXT,
    created_at TIMESTAMP NOT NULL,
    updated_at TIMESTAMP NOT NULL
);
```

### Refresh токены (Refresh Tokens)
```sql
CREATE TABLE refresh_tokens (
    id UUID PRIMARY KEY,
    user_id UUID REFERENCES users(id),
    token VARCHAR(500) UNIQUE NOT NULL,
    device_id VARCHAR(255) NOT NULL,
    expires_at TIMESTAMP NOT NULL,
    revoked BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP NOT NULL
);
```

### Комнаты чата (In-Memory)
Комнаты управляются в памяти `RoomService` с автоматической очисткой.

---

## 🚀 Деплой

### Docker
```dockerfile
# Сборка
docker build -t hubbly:latest .

# Запуск
docker run -p 5000:5000 \
  -e ConnectionStrings__DefaultConnection="Host=postgres;Port=5432;Database=hubbly;Username=postgres;Password=yourpassword" \
  -e Jwt__Secret="your-secret-key" \
  hubbly:latest
```

### GitHub Actions
Проект включает `.github/workflows/deploy.yml` для автоматического деплоя:
- Собирает при push в main
- Запускает тесты
- Деплоит на сервер через SSH
- Zero-downtime деплой с health checks

---

## 🧪 Тестирование

```bash
# Запустить все тесты
dotnet test

# Запустить с покрытием
dotnet test --collect:"XPlat Code Coverage"

# Конкретный проект
dotnet test Hubbly.Application.Tests/
```

---

## 🔒 Особенности безопасности

- **JWT токены** с коротким сроком (15 мин) и refresh ротацией
- **Отзыв refresh токенов** при logout или компрометации
- **Rate limiting** на всех endpoints
- **CORS** конфигурация из окружения
- **Валидация входных данных** на всех пользовательских вводах
- **Nonce валидация** для SignalR сообщений (защита от replay атак)
- **Глобальный обработчик исключений** с санитизированными ошибками

---

## 🐛 Известные проблемы и будущие улучшения

- [ ] Добавить unit тесты для SignalR хаба
- [ ] Реализовать персистентность сообщений (сейчас только в памяти)
- [ ] Добавить приватные сообщения
- [ ] Поддержка нескольких комнат с выбором пользователя
- [ ] Интеграция голосового чата
- [ ] Эмоции для аватаров
- [ ] Реакции/эмодзи на сообщения

---

## 🤝 Вклад в проект

1. Форкнуть репозиторий
2. Создать ветку фичи (`git checkout -b feature/AmazingFeature`)
3. Закоммитить изменения (`git commit -m 'Add AmazingFeature'`)
4. Запушить в ветку (`git push origin feature/AmazingFeature`)
5. Открыть Pull Request

**Стиль кода:**
- C# код следует стандартным Microsoft конвенциям
- Все публичные API должны иметь XML документацию
- Включены nullable reference types
- Async/await паттерны повсеместно

---

## 📄 Лицензия

Этот проект распространяется под лицензией MIT - смотрите файл [LICENSE](LICENSE) для деталей.

---

## 🙏 Благодарности

- [Three.js](https://threejs.org/) - 3D графика
- [SignalR](https://dotnet.microsoft.com/apps/aspnet/signalr) - Real-time функциональность
- [ASP.NET Core](https://dotnet.microsoft.com/apps/aspnet) - Web фреймворк
- [Polly](https://github.com/App-vNext/Polly) - Отказоустойчивость и обработка временных сбоев

---

## 📞 Поддержка

- Создавайте issue для багов или фич-запросов
- Читайте [wiki](https://github.com/yourusername/Hubbly.Backend/wiki) для документации
- Email: support@hubbly.example.com

---

**Сделано с ❤️ командой Hubbly**