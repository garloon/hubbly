# Hubbly Deployment Package

Этот пакет содержит все необходимое для развертывания Hubbly бэкенда на виртуальной машине Ubuntu 22.04.

## 📦 Содержимое пакета

```
deployment/
├── docker-compose.yml          # Оркестрация сервисов (PostgreSQL + API)
├── docker/
│   ├── Dockerfile.api         # Docker образ API
│   ├── .env                   # Переменные окружения (создается автоматически)
│   └── init-db.sql           # Инициализация БД
├── setup.sh                   # Скрипт первоначальной настройки VM
├── README.md                  # Этот файл
└── monitoring/                # (опционально) конфиги мониторинга
```

## 🚀 Быстрый старт

### 1. Подготовка виртуальной машины

Требования:
- Ubuntu 22.04 (или другой Linux)
- 2 CPU, 4 GB RAM, 50 GB NVMe
- SSH доступ с root привилегиями

### 2. Установка и настройка

На виртуальной машине выполните:

```bash
# Скопируйте папку deployment на VM
scp -r deployment/ root@89.169.46.33:/opt/hubbly/

# Запустите скрипт установки
ssh root@89.169.46.33 "cd /opt/hubbly && bash setup.sh"
```

Скрипт автоматически:
- Установит Docker и Docker Compose (если не установлены)
- Настроит переменные окружения
- Запустит PostgreSQL и API

### 3. Доступ к сервисам

После запуска:

| Сервис | Порт | Описание |
|--------|------|----------|
| **API** | `5000` | ASP.NET Core Web API |
| **PostgreSQL** | `5432` | База данных |

- **Swagger UI:** http://89.169.46.33:5000/swagger
- **API Health:** http://89.169.46.33:5000/health
- **SignalR Hub:** http://89.169.46.33:5000/chathub

## 🔄 Автоматический деплой через GitHub Actions

### Настройка GitHub Secrets

В репозитории на GitHub добавьте следующие secrets:

1. `SERVER_HOST` - IP адрес VM (например, `89.169.46.33`)
2. `SERVER_USER` - пользователь SSH (например, `root`)
3. `SERVER_PORT` - порт SSH (обычно `22`)
4. `SERVER_SSH_KEY` - приватный SSH ключ для доступа к VM
5. `DOCKER_USERNAME` - username Docker Hub
6. `DOCKER_PASSWORD` - пароль/токен Docker Hub

### Как это работает

При пуше в ветку `master`:

1. **Build & Push** job:
   - Собирает Docker образ API
   - Пушит образ в Docker Hub (`garloon/hubbly-api:latest`)

2. **Deploy** job (запускается после успешного билда):
   - Копирует `deployment/` папку на VM
   - Выполняет на VM:
     - Docker Hub login
     - `docker-compose pull` (скачивает свежий образ)
     - `docker-compose up -d` (перезапускает контейнеры)

### Ручной запуск деплоя

Если нужно деплоить вручную (не через CI/CD):

```bash
# На локальной машине (где есть docker)
cd deployment
docker-compose pull
docker-compose up -d
```

Или через скрипт на VM:

```bash
ssh root@89.169.46.33 "cd /opt/hubbly && bash setup.sh"
```

## 🔧 Управление

### Просмотр логов

```bash
ssh root@89.169.46.33 "cd /opt/hubbly && docker-compose logs -f"
```

### Остановка сервисов

```bash
ssh root@89.169.46.33 "cd /opt/hubbly && docker-compose down"
```

### Перезапуск сервисов

```bash
ssh root@89.169.46.33 "cd /opt/hubbly && docker-compose restart"
```

### Пересборка и деплой вручную (если нет CI/CD)

```bash
# Локально собрать образ
cd Hubbly.Api
docker build -t garloon/hubbly-api:latest .

# Запушить в Docker Hub
docker push garloon/hubbly-api:latest

# На VM обновить и перезапустить
ssh root@89.169.46.33 "cd /opt/hubbly && docker-compose pull && docker-compose up -d"
```

## 🔐 Безопасность

### Обязательно измените:

1. **JWT_SECRET** в `deployment/docker/.env`:
   ```bash
   openssl rand -base64 32
   ```

2. **POSTGRES_PASSWORD** в `deployment/docker/.env`

3. **CORS_ALLOWED_ORIGINS** - добавьте реальные домены

### Рекомендации:
- Используйте HTTPS (настройте Nginx reverse proxy с Let's Encrypt)
- Ограничьте доступ к портам через firewall (ufw)
- Регулярно обновляйте образы Docker
- Не открывайте порты 5000 и 5432 в firewall, если не нужно

## 💾 Резервное копирование

### Бэкап базы данных

```bash
ssh root@89.169.46.33 "docker-compose -f /opt/hubbly/docker-compose.yml exec -T postgres pg_dump -U hubbly hubbly" > backup_$(date +%Y%m%d_%H%M%S).sql
```

### Восстановление базы данных

```bash
cat backup_20260220.sql | ssh root@89.169.46.33 "docker-compose -f /opt/hubbly/docker-compose.yml exec -T postgres psql -U hubbly hubbly"
```

## 📝 Миграции базы данных

Миграции EF Core автоматически применяются при первом запуске через `init-db.sql`.

Для ручного применения:
```bash
ssh root@89.169.46.33 "docker-compose -f /opt/hubbly/docker-compose.yml exec api dotnet ef database update"
```

## 🔄 Перенос на другую машину

1. Скопируйте всю папку `/opt/hubbly` на новую машину
2. Обновите IP-адреса в `deployment/docker/.env` (CORS_ALLOWED_ORIGINS)
3. Запустите `setup.sh` на новой машине
4. Обновите `SERVER_HOST` в GitHub Secrets

## 🏗️ Архитектура

### Docker Compose Services

| Service | Образ | Порт | Описание |
|---------|-------|------|----------|
| `postgres` | postgres:15-alpine | 5432 | База данных |
| `api` | garloon/hubbly-api:latest | 5000 | ASP.NET Core API |

### Сети
- `hubbly-network` - bridge сеть для внутренней коммуникации

### Volumes
- `postgres_data` - данные PostgreSQL

## 🐛 Troubleshooting

### API не запускается
```bash
# Проверить логи
ssh root@89.169.46.33 "cd /opt/hubbly && docker-compose logs api"

# Проверить миграции
ssh root@89.169.46.33 "cd /opt/hubbly && docker-compose exec api dotnet ef database update"
```

### Контейнеры не стартуют после перезагрузки
```bash
# Перезапустить вручную
ssh root@89.169.46.33 "cd /opt/hubbly && docker-compose up -d"
```

### Ошибка подключения к БД
1. Проверьте, что PostgreSQL запущен: `docker ps | grep postgres`
2. Проверьте логи: `docker-compose logs postgres`
3. Проверьте переменные в `.env`

### Высокое потребление памяти
На VM с 4GB RAM:
- API: ~300-500MB
- PostgreSQL: ~500MB
- Итого ~1GB, остается для ОС

Если нужно уменьшить потребление:
1. Ограничьте память PostgreSQL в docker-compose.yml
2. Уменьшите память API через `--memory` флаги

## 📚 Документация

- [ASP.NET Core](https://docs.microsoft.com/aspnet/core/)
- [Docker Compose](https://docs.docker.com/compose/)
- [GitHub Actions](https://docs.github.com/en/actions)

## 🆘 Поддержка

При проблемах:
1. Проверьте логи всех сервисов
2. Убедитесь, что все контейнеры запущены: `docker ps`
3. Проверьте свободное место: `df -h`
4. Проверьте память: `free -h`

---

**Версия:** 2.0.0
**Дата:** 2026-02-20
**Автор:** Roo AI Assistant

**Изменения:**
- Убраны все компоненты мониторинга (Netdata, Prometheus, Grafana, Loki, Tempo, OTel Collector)
- Добавлена автоматизация через GitHub Actions
- Упрощена конфигурация
- Минимальный footprint - только PostgreSQL + API
